import random

import logger

_filepath = ''

def init(
  filename:str
):
  '''
  初期化
  - ファイルパス記録
  - ファイルに既に値が入っていれば読み出し
  '''

def run(
  solution
):
  '''
  解提出
  '''
  logger.log_info(f'[solution-submit] {solution}')
  score = random.random()
  logger.log_info(f'[solution score] {"todo"}')
  return score
